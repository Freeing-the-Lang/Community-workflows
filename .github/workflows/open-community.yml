name: 🌍 Open Community + ProofLedger + Auto Discussion Enable

on:
  push:
    branches: [main]
  workflow_dispatch:

# 🚨 PREVENTS INFINITE LOOPS: Stops the bot from triggering itself
if: github.actor != 'github-actions[bot]'

jobs:
  community:
    name: 📦 Ensure Community Standards
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: 🏗️ Create Missing Community Files
        shell: bash
        run: |
          mkdir -p .github/ISSUE_TEMPLATE
          echo "🔍 Checking community standard files..."

          # --- README ---
          if [ ! -f README.md ]; then
            echo "# Freeing-the-Lang" > README.md
            echo "Language unification ecosystem — automated community setup." >> README.md
          fi

          # --- LICENSE ---
          if [ ! -f LICENSE ]; then
            echo "MIT License" > LICENSE
            echo "Copyright (c) $(date +%Y) Freeing-the-Lang" >> LICENSE
          fi

          # --- CODE OF CONDUCT ---
          if [ ! -f CODE_OF_CONDUCT.md ]; then
            printf "# Code of Conduct\nWe follow the Contributor Covenant Code of Conduct.\n- Be respectful and constructive.\n- No harassment or hate speech.\n- Collaborate openly and fairly.\n" > CODE_OF_CONDUCT.md
          fi

          # --- CONTRIBUTING ---
          if [ ! -f CONTRIBUTING.md ]; then
            printf "# Contributing\n1. Fork and branch.\n2. Commit clearly.\n3. All pushes automatically update ProofLedger and Discussions.\n" > CONTRIBUTING.md
          fi

          # --- SECURITY ---
          if [ ! -f SECURITY.md ]; then
            printf "# Security Policy\nReport vulnerabilities privately.\nResponse target: within 48 hours.\n" > SECURITY.md
          fi

          # --- BUG REPORT TEMPLATE ---
          if [ ! -f .github/ISSUE_TEMPLATE/bug_report.md ]; then
            printf -- "---\nname: 🐞 Bug Report\nabout: Report a problem\nlabels: bug\n---\n### Description\nDescribe the issue.\n### Steps to Reproduce\n1.\n2.\n3.\n### Expected Behavior\nDescribe what you expected.\n" > .github/ISSUE_TEMPLATE/bug_report.md
          fi

          # --- PR TEMPLATE ---
          if [ ! -f .github/PULL_REQUEST_TEMPLATE.md ]; then
            printf "# 🧩 Pull Request\n## Summary\n## Related Issue\n- [ ] Build successful\n- [ ] ProofLedger updated\n- [ ] Linked Discussions/Issues\n" > .github/PULL_REQUEST_TEMPLATE.md
          fi

          # --- GIT COMMIT ---
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md LICENSE CODE_OF_CONDUCT.md CONTRIBUTING.md SECURITY.md .github
          git commit -m "chore: 🧩 Auto ensure community standard files" || echo "No changes"
          git push origin main || echo "Push skipped"

  proofledger:
    name: 🧾 ProofLedger Update
    runs-on: ubuntu-latest
    needs: community
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: 🔒 Append ProofLedger Entry
        shell: bash
        run: |
          mkdir -p proofledger
          {
            echo "Commit SHA: $GITHUB_SHA"
            echo "Author: $GITHUB_ACTOR"
            echo "Repository: $GITHUB_REPOSITORY"
            echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            echo "SHA256: $(git rev-parse HEAD | sha256sum | cut -d' ' -f1)"
            echo "--------------------------------------"
          } >> proofledger/proofledger.txt
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add proofledger/proofledger.txt
          git commit -m "chore: 🔒 Update ProofLedger for $GITHUB_SHA" || echo "No new ledger entry"
          git push origin main || echo "Push skipped"

  enable-discussions:
    name: ⚙️ Enable Discussions Automatically
    runs-on: ubuntu-latest
    permissions:
      administration: write
      discussions: write
    steps:
      - uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split("/");
            console.log("🔧 Enabling Discussions feature if disabled...");
            try {
              await github.request("PATCH /repos/{owner}/{repo}", {
                owner,
                repo,
                has_discussions: true
              });
              console.log("✅ Discussions feature is enabled.");
            } catch (e) {
              console.error("Failed to enable discussions. Check 'administration: write' permission.", e.message);
            }
            
            console.log("🔎 Checking for 'General' category...");
            const repoData = await github.graphql(`
              query($owner:String!, $repo:String!){
                repository(owner:$owner, name:$repo){
                  id
                  discussionCategories(first:10){nodes{id name}}
                }
              }`,
              { owner, repo }
            );

            const general = repoData.repository.discussionCategories.nodes.find(c => c.name === "General");
            if (general) {
              console.log("✅ 'General' category exists.");
            } else {
              console.log("➕ Creating 'General' category...");
              try {
                const createCat = await github.graphql(`
                  mutation($repoId:ID!){
                    createDiscussionCategory(input:{
                      repositoryId:$repoId,
                      name:"General",
                      description:"General discussion topics"
                    }){discussionCategory{id name}}
                  }`, { repoId: repoData.repository.id });
                console.log("✅ Created:", createCat.createDiscussionCategory.discussionCategory.name);
              } catch (e) {
                 console.error("Failed to create 'General' category. Check 'discussions: write' permission.", e.message);
              }
            }

  discussion:
    name: 💬 Create Discussion for Commit
    runs-on: ubuntu-latest
    needs: enable-discussions
    permissions:
      discussions: write
    steps:
      - uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split("/");
            const repoInfo = await github.graphql(`
              query($owner: String!, $repo: String!) {
                repository(owner: $owner, name: $repo) {
                  id
                  discussionCategories(first: 10) { nodes { id name } }
                }
              }
            `, { owner, repo });
            
            const category = repoInfo.repository.discussionCategories.nodes.find(c => c.name === "General");
            if (!category) {
              console.error("❌ 'General' category not found. Skipping discussion creation.");
              return;
            }

            const title = `🗣️ Commit Discussion — ${process.env.GITHUB_SHA.slice(0,7)}`;
            const body = [
              `A new commit was pushed by @${process.env.GITHUB_ACTOR}.`,
              `**Repository:** ${process.env.GITHUB_REPOSITORY}`,
              `**Commit:** ${process.env.GITHUB_SHA}`,
              `**Message:** \`\`\`${process.env.GITHUB_EVENT_PATH ? (await require('fs').promises.readFile(process.env.GITHUB_EVENT_PATH, 'utf8').then(data => JSON.parse(data).head_commit.message)) : 'N/A'}\`\`\``
            ].join("\n\n");
            
            const result = await github.graphql(`
              mutation($repoId: ID!, $catId: ID!, $title: String!, $body: String!) {
                createDiscussion(input:{
                  repositoryId:$repoId,
                  categoryId:$catId,
                  title:$title,
                  body:$body
                }){discussion{url}}
              }`,
              { repoId: repoInfo.repository.id, catId: category.id, title, body }
            );
            console.log("✅ Discussion created:", result.createDiscussion.discussion.url);

  issue:
    name: 🪶 Auto Create Issue
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: 🚀 Create Issue for Commit
        uses: peter-evans/create-issue@v5
        with:
          title: "🚀 Auto Update — ${{ github.event.head_commit.message }}"
          body: |
            A new commit was pushed to `main` by @${{ github.actor }}.
            
            **Commit:** `${{ github.sha }}`
            **Message:** `${{ github.event.head_commit.message }}`
            
            This issue was auto-generated for tracking and proof-of-work.
          labels: |
            automation
            proofledger
          assignees: ${{ github.actor }}
